{
  "name": "@ronda/server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "echo \"TODO(P5): arrancar el servidor con node --watch\" && exit 1",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "start": "echo \"TODO(P5): arrancar en producción\" && exit 1",
    "sim": "echo \"TODO(P9): simulador con bots\" && exit 1",
    "db:migrate": "echo \"TODO(P7): aplicar migraciones\" && exit 1"
  },
  "dependencies": {
    "@ronda/engine": "workspace:*",
    "@ronda/protocol": "workspace:*",
    "pg": "^8.13.1",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/pg": "^8.11.10"
  }
}
