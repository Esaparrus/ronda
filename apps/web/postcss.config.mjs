// Tailwind v4 se configura en CSS (ver src/styles/globals.css), no aquí.
// Este fichero solo engancha el plugin de PostCSS. Contrato P10.
export default {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};
