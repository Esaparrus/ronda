// @ronda/web
//
// Front Next.js App Router + Tailwind + Zustand. Solo pinta vistas censuradas que
// manda el servidor; nunca decide nada de reglas. Se implementa a partir de P10.
// Esta página existe para que el monorepo arranque.

export default function Page() {
  return (
    <main>
      <p>Ronda — web (placeholder de P0, se implementa en P10).</p>
    </main>
  );
}
